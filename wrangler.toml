name = "proxdocs"
pages_build_output_dir = "dist"
compatibility_date = "2025-01-01"

# The builder Functions bundle typescript, which reaches for a handful of node
# builtins. Without this flag /api/preview and /api/download fail at build time.
compatibility_flags = ["nodejs_compat"]
